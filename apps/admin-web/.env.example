# NEXT_PUBLIC_API_URL is inlined at BUILD time, not read at runtime.
# If you change it, you must rebuild (`npm run admin:build` or rebuild the
# Docker image) — restarting the server alone will not pick up a new value.
NEXT_PUBLIC_API_URL=http://localhost:3000
